// Flip to `true` to re-enable login UI everywhere (web + iOS).
// Disabled for the initial App Store submission to avoid review issues.
// When re-enabling, also ensure: Sign in with Apple is implemented,
// in-app account deletion exists, privacy policy is updated, and the
// App Privacy disclosures in App Store Connect are updated.
export const LOGIN_ENABLED = false;

// Flip to `true` to re-enable the restaurant booking form (collects
// guest name, phone, email). Disabled for the initial App Store
// submission to remove contact-data collection from the app entirely.
// When re-enabling, also update the privacy policy and App Privacy
// disclosures in App Store Connect to declare Name/Email/Phone again.
export const BOOKING_ENABLED = false;
